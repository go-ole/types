/*
Package iunknown has every COM interface inherits from IUknown interface and
therefore has the following methods:

	AddRef()
	QueryInterface()
	Release()

These methods are the first entries on all of the virtual tables for COM
objects.
*/
package iunknown
